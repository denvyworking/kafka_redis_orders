kafka:
  brokers:
    - "localhost:9092"
  topic: "orders"
  group_id: "order-consumer-group"

redis:
  addr: "localhost:6379"
  order_ttl: "1h"

http:
  port: "8080"

retry:
  initial_interval: "200ms"
  max_interval: "5s"
  max_elapsed_time: "30s"
  multiplier: 2.0