kafka:
  brokers:
    - "kafka:9092"
  topic: "orders"
  group_id: "order-consumer-group"

redis:
  addr: "redis:6379"
  order_ttl: "24h"

http:
  port: "8080"

retry:
  initial_interval: "200ms"
  max_interval: "5s"
  max_elapsed_time: "30s"
  multiplier: 2.0